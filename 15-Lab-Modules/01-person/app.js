// Obj 'result' required for testing in Judge

let Person = require('./person').Person;    // with module.exports = {Person}
result.Person = Person;

// let Person = require('./person');        // with module.exports = Person
// result.Person = Person;